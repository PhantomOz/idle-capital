{
  "name": "idle-capital",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.20.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit -p packages/core && tsc --noEmit -p packages/obligations && tsc --noEmit -p packages/kernel && tsc --noEmit -p packages/yields",
    "test:live": "vitest run packages/yields/test/live.integration.test.ts"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^9.6.0",
    "@types/node": "^22.7.0",
    "better-sqlite3": "^13.0.3",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
