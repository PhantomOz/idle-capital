# Attribution

ETHGlobal require documentation of "which parts of the code, specific files, or
assets were generated or assisted by AI." This file is that record. It is
maintained from the first commit onward, per file, as work lands — not
reconstructed at the end.

## Summary

**This build is AI-assisted throughout.** Claude Code (Claude Opus 5) wrote the
large majority of the implementation, directed by PhantomOz through a
spec-driven workflow. All specs, prompts and planning artifacts are committed in
`/specs` and `/prompts` so the direction is inspectable, not just the output.

Human contribution is concentrated in the places it belongs: problem definition,
architectural judgment, the track-requirements analysis that shaped scope, the
decisions recorded in `DECISIONS.md`, review of every change, and the demo
video — which is narrated by a team member and is not AI-generated.

## Legend

| Mark | Meaning |
|---|---|
| `AI` | Generated by Claude Code, reviewed by a team member |
| `AI+H` | Generated by Claude Code, then materially edited by a team member |
| `H` | Written by a team member |
| `H+AI` | Written by a team member with AI assistance on parts |

## Per-file record

| File / path | Mark | Note |
|---|---|---|
| `ATTRIBUTION.md` | `AI+H` | Structure AI-generated; content reviewed and owned by the team |
| `DECISIONS.md` | `H+AI` | Decisions are the team's; AI drafted the write-ups from team direction |
| `.gitignore` | `AI` | Standard ignores |
| `specs/` | `AI+H` | Specs drafted by AI from team requirements, reviewed and approved before build |
| `prompts/` | `H` | Verbatim session prompts issued by the team |
| `specs/plans/` | `AI+H` | Implementation plans drafted by AI; task decomposition reviewed and approved |
| `package.json`, `pnpm-workspace.yaml`, `tsconfig.base.json`, `vitest.config.ts` | `AI` | Workspace scaffolding |
| `packages/core/**` | `AI` | Domain types and money helpers; reviewed by the team |
| `packages/obligations/**` | `AI` | FX table and buffer math; rates and rounding direction reviewed by the team |
| `packages/kernel/**` | `AI` | Invariants K1-K8 and validate(); invariant semantics and the veto/escalate split are team decisions (see DECISIONS.md D-006) |
| `packages/yields/**` | `AI` | Registry, standardized query, normalization and client; registry contents and allowlist policy are team decisions (D-010) |
| `specs/spikes/` | `AI+H` | Spike probes written and run by AI; the GO/no-go call and its consequences are the team's |
| `packages/ledger/**` | `AI` | Schema, state machine, intent ledger and recovery; the never-resubmit rule is a team decision (D-003) |
| `apps/api/**` | `AI` | Ports, orchestrator, HTTP surface; policy defaults and the allowlist are team decisions (D-010) |
| `apps/web/**` | `AI` | Ledger-sheet UI, formatters and components; visual direction and the D-013 restructure reviewed against the running page |
| `packages/agent/**` | `AI` | Prompt, forced-tool schema and defensive parsing; the prompt's framing is a team decision |
| `packages/chain/**` | `AI` | Arc client; the native-USDC finding came from probing the live chain |
| `packages/wallet/**` | `AI` | Privy client, treasury and executor; the two-ceiling design is a team decision (D-015) |

*(Rows are appended as files land. See git history for the ordering.)*

## Commit trailers

Commits carry `Co-Authored-By` trailers naming the AI model. Those trailers are
disclosure, not the attribution mechanism — this file is. Commit *authorship*
is the team member who directed and reviewed the work.
